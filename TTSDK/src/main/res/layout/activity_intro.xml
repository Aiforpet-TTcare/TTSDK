<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".IntroActivity">



    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <Button
            android:layout_width="200dp"
            android:layout_height="40dp"
            android:text="눈"
            android:id="@+id/eyeBtn" />
        <Button
            android:layout_width="200dp"
            android:layout_height="40dp"
            android:text="관절"
            android:id="@+id/kneeBtn" />

        <Button
            android:layout_width="200dp"
            android:layout_height="40dp"
            android:text="치아 Onnx Side"
            android:id="@+id/onnxTeethSideBtn" />
        <Button
            android:layout_width="200dp"
            android:layout_height="40dp"
            android:text="치아 Onnx Center"
            android:id="@+id/onnxTeethCenterBtn" />
        <Button
            android:layout_width="200dp"
            android:layout_height="40dp"
            android:text="치아 Onnx Segment"
            android:id="@+id/onnxSegmentBtn" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>